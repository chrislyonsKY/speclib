"""speclib FastAPI server — optional self-hosted API.

Provides REST endpoints and Jinja-templated HTML pages
for the spectral library. Reads from Parquet query layer.

Usage:
    speclib serve
    # or: uvicorn web.api.main:app --reload
"""

from __future__ import annotations

# TODO: Import FastAPI, Jinja2Templates
# TODO: Configure CORS
# TODO: Mount static files
# TODO: Include route modules
# TODO: Configure Parquet data path from env/config

# app = FastAPI(title="speclib", version="0.1.0")
